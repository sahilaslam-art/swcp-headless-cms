{
  "name": "sahil-aslam-portfolio-monorepo",
  "version": "1.0.0",
  "description": "Unified repository containing the 3D Portfolio, Admin Panel, and Backend API.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix admin-panel",
    "dev:server": "npm run dev --prefix server",
    "dev:admin": "npm run dev --prefix admin-panel",
    "dev": "concurrently -c \"cyan.bold,magenta.bold\" -n \"SERVER,ADMIN\" \"npm run dev:server\" \"npm run dev:admin\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
